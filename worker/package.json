{
  "name": "filekind-analytics-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply filekind-analytics --local",
    "db:migrate:remote": "wrangler d1 migrations apply filekind-analytics --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260915.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.38.0"
  }
}
